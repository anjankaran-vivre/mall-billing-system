version: '3.8'
services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./:/app:cached
      - node_modules:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    env_file:
      - .env
    command: npm run dev -- --host 0.0.0.0

volumes:
  node_modules:
